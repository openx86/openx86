+# Shared testbench sources (optional).
+#
+# This repo historically keeps most `*_tb.sv` next to their DUT under `rtl/`.
+# As we migrate TBs into `tb/`, this file can be used to list common TB
+# utilities (clock/reset generators, scoreboards, bus functional models, etc).
+
+# Intentionally empty for now.
